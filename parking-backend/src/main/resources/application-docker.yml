spring:
  datasource:
    url: jdbc:mysql://mysql:3306/smart_parking?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root123456
  data:
    redis:
      host: redis
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/init.sql
      data-locations: classpath:sql/data.sql
